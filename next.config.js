/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // todo: remove then
    domains: ["cdn-icons-png.flaticon.com", "eventsmadeira.com", 'img.evbuc.com', 'images.unsplash.com', 'fp9z7zqeiybyeset.public.blob.vercel-storage.com'],
  },
};

module.exports = nextConfig;
